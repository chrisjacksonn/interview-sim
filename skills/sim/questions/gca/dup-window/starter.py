"""Repeat Alerts.

Read problem.md for the full statement.
"""


def solve(alerts, window):
    """Names that fired twice within `window` of each other.

    Args:
        alerts: list of (name, timestamp) pairs, in no particular order.
        window: the inclusive gap that counts as noisy.

    Returns:
        A sorted list of the noisy names, each appearing once.
    """
    # Your code here.
    raise NotImplementedError
