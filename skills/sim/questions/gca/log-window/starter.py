"""Log Window.

Read problem.md for the full statement.
"""


def solve(counts, width):
    """Largest total over any run of `width` consecutive buckets.

    Args:
        counts: per-bucket error counts, in order.
        width: how many consecutive buckets to total.

    Returns:
        The largest such total, or 0 when no window of that width exists.
    """
    # Your code here.
    raise NotImplementedError
